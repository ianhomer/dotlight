#!/usr/bin/env bash

#
# Install with apt
#

set -e

if [ $(sudo -n -l apt) = "/usr/bin/apt" ]; then
  sudo apt update
  sudo apt install -y \
    git \
    stow \
    vim
else
  echo "user does not have permission to run apt."
  echo "consider add entry in /etc/sudoers"
  exit
fi
