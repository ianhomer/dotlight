#!/usr/bin/env bash

$(shim) && cd ${DOTCORE_DIR} && . bin/i.sh

DOTFILES_DIR=${DOTCORE_DIR}/dotfiles

log:: "stowing dotfiles"

cd ${DOTFILES_DIR}
for DIR in $(ls -d *); do
  log:: "... stowing $DIR"
  stow -v --dotfiles -t $HOME $DIR
done
