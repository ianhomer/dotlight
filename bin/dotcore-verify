#!/usr/bin/env bash

#
# Verify dotme install AOK
#

set -e

$(shim) && cd ${DOTCORE_DIR} && . bin/i.sh

if [[ "$DOTCORE_LOADED" != "y" ]]; then
  log::exit "dotcore not loaded"
  exit 1
fi

log:: OK dotcore
